# Key-user training

Key users learn to read error category and order reference, decide whether the
cause is data or technical, and use the approval choice consistently. They do
not retry validation failures blindly: fix the source data first. Training also
covers checking the BC exception list, locating the WMS tracking result and
recording the root cause for recurring incidents.
